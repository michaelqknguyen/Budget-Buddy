# PostgreSQL
# ------------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=budgetbuddy
POSTGRES_USER=budgetbuddy
POSTGRES_PASSWORD=budgetbuddy_local_dev_password

# DATABASE_URL (for Django)
DATABASE_URL=postgres://budgetbuddy:budgetbuddy_local_dev_password@postgres:5432/budgetbuddy
